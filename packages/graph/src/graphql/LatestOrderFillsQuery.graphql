query LatestOrderFillsQuery($marketEntityId: String!) {
  fillOrderEvents(
    where: { market: $marketEntityId, isTaker: true }
    orderBy: blockTime
    orderDirection: desc
    first: 100
  ) {
    blockTime
    quoteDeltaX18
    amountDeltaX18
    order {
      priceX18
    }
  }
}
