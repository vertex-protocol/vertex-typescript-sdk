query LatestOrderFillsQuery($productId: BigInt!) {
  fillOrderEvents(
    # We want to show maker order prices
    where: { productId: $productId, isTaker: false }
    orderBy: blockTime
    orderDirection: desc
    first: 100
  ) {
    blockTime
    quoteDelta
    amountDelta
    order {
      priceX18
    }
  }
}
