{
  "name": "@vertex/graph",
  "version": "0.1.0",
  "description": "> TODO: description",
  "author": "Frank Jia <frank@vertexprotocol.com>",
  "homepage": "",
  "license": "ISC",
  "main": "dist/src/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist && rm -r tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "dev": "tsc -w",
    "generate-graph-client": "rm -rf src/generated && graphclient build && mv .graphclient src/generated",
    "test": "ts-node src/client/test.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "devDependencies": {
    "@graphprotocol/client-cli": "^2.1.3",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "graphql": "^16.5.0"
  }
}
